# ShambaPoint Frontend Configuration (Vite)
# Supabase removed — auth/data is Flask + MySQL only.
# Auth is handled by POST /api/auth/login and POST /api/auth/register (see backend/app.py).

# Point to your local Flask backend in development (port 5000), or your production API URL.
VITE_API_URL=http://localhost:5000
